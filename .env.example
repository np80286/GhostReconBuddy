# Local development only. Replace the password in BOTH values before starting.
POSTGRES_PASSWORD=replace-with-a-local-password
DATABASE_URL=postgresql://ghost_recon_buddy:replace-with-a-local-password@127.0.0.1:5432/ghost_recon_buddy
